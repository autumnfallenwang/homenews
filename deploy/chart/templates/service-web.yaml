apiVersion: v1
kind: Service
metadata:
  name: {{ include "homenews.fullname" (dict "context" . "component" "web") }}
  labels:
    {{- include "homenews.labels" (dict "context" . "component" "web") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.web.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "homenews.selectorLabels" (dict "context" . "component" "web") | nindent 4 }}
