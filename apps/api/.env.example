DATABASE_URL=postgresql://homenews:homenews@localhost:5433/homenews
CORS_ORIGINS=http://localhost:3000
LLM_GATEWAY_URL=http://localhost:51277

# Note: most tunable values (weights, lambdas, tag vocabulary, scheduler config,
# LLM model selection) live in the settings DB table, not env vars.
# See DEFAULT_SETTINGS in @homenews/shared for the full list.
# LLM model keys available:
#   llm_model_scoring / llm_model_scoring_fallback
#   llm_model_clustering / llm_model_clustering_fallback
#   llm_model_summarization / llm_model_summarization_fallback
